
    
    function effet1(element) {
        element.style.transform = 'scale(1.05)';
        element.style.transition = 'transform 0.3s ease';
    }

    
    function effet2(element) {
        element.style.transform = 'scale(1)';
    }

    
    function effet3(sectionName) {
        alert("Vous explorez maintenant les " + sectionName);
    }
